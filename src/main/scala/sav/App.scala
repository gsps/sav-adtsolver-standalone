package sav

object App {
  def main(args: Array[String]) = {
//    val solver = new AdtSolver()
//
//    val sigList = Seq(Seq(), Seq())
//    val sig = solver.Signature(Seq(sigList))
//    val inst = solver.Instance(sig, Map(), Seq(), Seq(), Seq(), Seq())
//    println(solver.solve(inst))
//
//    val inst2 = solver.Instance(sig, Map(), Seq((solver.Variable(1), solver.Variable(1))), Seq(), Seq(), Seq())
//    println(solver.solve(inst2))
//
//    val inst3 = solver.Instance(sig, Map(), Seq((solver.Variable(1), solver.Variable(1))),
//      Seq((solver.Variable(1), solver.Variable(1))), Seq(), Seq())
//    println(solver.solve(inst3))
  }
}
